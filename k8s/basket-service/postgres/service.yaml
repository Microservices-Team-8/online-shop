apiVersion: v1
kind: Service
metadata:
  name: basket-service-postgres
spec:
  type: ClusterIP
  selector:
    app: basket-service-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432