apiVersion: v1
kind: Service
metadata:
  name: users-service-postgres
spec:
  type: NodePort
  selector:
    app: users-service-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
      nodePort: 30001