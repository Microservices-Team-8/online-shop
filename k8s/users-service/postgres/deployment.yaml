apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-service-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-service-postgres
  template:
    metadata:
      labels:
        app: users-service-postgres
    spec:
      containers:
        - name: users-service-postgres
          image: postgres:14.1-alpine3.15
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          envFrom:
            - configMapRef:
                name: users-service-postgres-config
            - secretRef:
                name: users-service-postgres-secrets
          volumeMounts:
            - mountPath: /var/lib/users-postgresql/data/
              name: users-postgredb
      volumes:
        - name: users-postgredb
          persistentVolumeClaim:
            claimName: users-service-storage